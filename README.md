# OPE Logger - Revenue Intelligence System

Sistema de optimización de conversión y reducción de devoluciones para e‑commerce mediante mensajes contextuales y evaluación off‑policy.

## Instalación

```bash
npm install
npm start

Guardá (`Ctrl+O`, `Enter`, `Ctrl+X`).

---

## 🚫 3. Editar `.gitignore` (para no subir archivos sensibles)

```bash
nano .gitignore
.env
ope.db*
node_modules/
logs/
data/
.DS_Store
*.log
*.save
*.save.*

